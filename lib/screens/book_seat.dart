import 'package:flutter/material.dart';

class BookSeatPage extends StatelessWidget {
  const BookSeatPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
      body: Center(
        child: Text('Book Seat Page'),
      ),
    );
  }
}
